<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Test" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- New background color for the entire window -->
        <item name="android:windowBackground">@color/ivory</item>
    </style>

    <style name="Theme.Test" parent="Base.Theme.Test" />

    <color name="customWindowBackground">#FAFAD2</color>

    <style name="Theme.Test.Fullscreen" parent="Theme.Test">
        <item name="android:actionBarStyle">@style/Widget.Theme.Test.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.Vellog_login.FullscreenContainer" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="ThemeOverlay.Test.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

</resources>

